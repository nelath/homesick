#! /bin/bash

if [[ -x "${GOPATH}/bin/go1.20.4" ]]; then
	GO_BIN=${GOPATH}/bin/go1.20.4
elif [[ -x "/usr/local/bin/go" ]]; then
	GO_BIN=/usr/local/bin/go
elif [[ -x "/usr/bin/go" ]]; then
	GO_BIN=/usr/bin/go
else
	GO_BIN=/bin/false
fi

#shellcheck disable=2086,2048
"${GO_BIN}" $*
